package com.example.alison.autprime;

import android.content.Context;
import static org.junit.Assert.*;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.runners.MockitoJUnitRunner;

import static org.hamcrest.core.Is.is;

//Remember to remove the comments to start the login's unit test
@RunWith(MockitoJUnitRunner.class)
public class LoginActivityTest {
    /*private static final String EXPECTED_RESULT = "Login was successful";

    @Mock
    Context mockContent;

    @Test
    public void readStringFromContext_LocalizedString(){
        MainActivity myTest = new MainActivity(mockContent);

        //tests whether user provides input for both username and password
        String result = myTest.validate("cvbb345","zdvvb");
        assertThat(result,is(EXPECTED_RESULT));
    }*/
}